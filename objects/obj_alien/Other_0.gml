/// @description Triple pace to return quickly
floatTowards(self, obj_ball, acceleration, maxSpeed);
floatTowards(self, obj_ball, acceleration, maxSpeed);
floatTowards(self, obj_ball, acceleration, maxSpeed);